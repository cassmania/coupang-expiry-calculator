# 구현 계획

## 목표

Genspark에 게시된 `쿠팡 유통기한 계산기`의 핵심 계산 흐름을 유지하면서, GitHub Pages에서 바로 배포할 수 있는 단일 정적 웹사이트로 정리한다.

## 요청 반영

1. 화면 전체를 밝은 배경의 라이트 모드로 고정한다.
2. 하단에 `made by 김기범`을 기존 안내 문구의 약 2배 크기로 표시한다.
3. 제작자 문구는 쿠팡 로고를 연상시키는 빨강·주황·노랑·초록·파랑 계열 색상을 글자별로 적용한다.
4. 외부 로고 이미지가 깨지는 문제를 피하기 위해 헤더 로고를 HTML/CSS 텍스트로 구성한다.
5. `index.html`만으로 실행되게 만들어 GitHub Pages에 그대로 올릴 수 있게 한다.

## 성공 기준

- PC와 390px 모바일 화면에서 가로 스크롤 없이 표시된다.
- 제조일자와 유효기간을 입력하면 `제조일자 + 유효기간 - 1일`로 유통기한이 계산된다.
- 초기화와 결과 복사 버튼이 동작한다.
- 브라우저/운영체제의 다크 모드 설정과 관계없이 라이트 모드로 보인다.
- GitHub Pages에서 별도 빌드 과정 없이 `index.html`이 열린다.

## 검증 순서

1. 정적 HTML/CSS/JavaScript 구문 확인
2. 로컬 웹 서버에서 계산·초기화·복사 동작 확인
3. 데스크톱과 390px 모바일 화면 캡처 및 레이아웃 확인

