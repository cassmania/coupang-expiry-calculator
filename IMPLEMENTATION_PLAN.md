# 구현 계획

## 목표

Genspark에 게시된 `쿠팡 유통기한 계산기`의 핵심 계산 흐름을 유지하면서, GitHub Pages에서 바로 배포할 수 있는 단일 정적 웹사이트로 정리한다.

## 요청 반영

1. 화면 전체를 밝은 배경의 라이트 모드로 고정한다.
2. 하단에 `made by 김기범`을 기존 안내 문구의 약 2배 크기로 표시한다.
3. 제작자 문구는 쿠팡 로고를 연상시키는 빨강·주황·노랑·초록·파랑 계열 색상을 글자별로 적용한다.
4. 외부 로고 이미지가 깨지는 문제를 피하기 위해 헤더 로고를 HTML/CSS 텍스트로 구성한다.
5. `index.html`만으로 실행되게 만들어 GitHub Pages에 그대로 올릴 수 있게 한다.

## 성공 기준

- PC와 390px 모바일 화면에서 가로 스크롤 없이 표시된다.
- 제조일자와 유효기간을 입력하면 `제조일자 + 유효기간 - 1일`로 유통기한이 계산된다.
- 초기화와 결과 복사 버튼이 동작한다.
- 브라우저/운영체제의 다크 모드 설정과 관계없이 라이트 모드로 보인다.
- GitHub Pages에서 별도 빌드 과정 없이 `index.html`이 열린다.

## 검증 순서

1. 정적 HTML/CSS/JavaScript 구문 확인
2. 로컬 웹 서버에서 계산·초기화·복사 동작 확인
3. 데스크톱과 390px 모바일 화면 캡처 및 레이아웃 확인

## PWA 홈 화면 바로가기 추가

### 구현 범위

1. `manifest.json`에 앱 이름, 시작 경로, 독립 실행 모드, 192px·512px 아이콘을 정의한다.
2. `sw.js`에서 앱 파일을 미리 저장하고, 네트워크가 끊겨도 계산기 또는 `offline.html`을 연다.
3. 기존 계산 JavaScript를 `app.js`로 분리하고 서비스 워커 등록과 설치 버튼 동작을 함께 관리한다.
4. Android 계열 브라우저에서는 설치 팝업을 열고, iPhone Safari에서는 공유 메뉴의 `홈 화면에 추가` 안내를 표시한다.
5. 이미 홈 화면 앱으로 실행 중이면 중복 설치 안내를 표시하지 않는다.

### 성공 기준

- 공개 HTTPS 주소에서 manifest와 서비스 워커가 정상 등록된다.
- manifest의 192px·512px PNG 아이콘이 모두 로드된다.
- 홈 화면 추가 버튼이 기존 모바일 한 화면 레이아웃을 깨뜨리지 않는다.
- 계산·초기화·복사 기능이 JavaScript 분리 후에도 동일하게 동작한다.
- 서비스 워커 캐시가 준비된 뒤 오프라인 재접속 시 계산기 화면이 열린다.
- Android 설치 이벤트와 iPhone 수동 설치 안내를 각각 처리한다.

## 카카오톡 링크의 홈 화면 추가 개선

### 구현 범위

1. 카카오톡 내부 브라우저를 감지해 일반 설치 안내 대신 외부 브라우저 안내를 보여준다.
2. Android 카카오톡에서는 사용자가 버튼을 눌렀을 때 Chrome으로 현재 사이트를 여는 Intent 링크를 실행한다.
3. iPhone 카카오톡에서는 카카오톡 메뉴의 `다른 브라우저로 열기` 안내와 링크 복사 기능을 제공한다.
4. 카카오톡에서 처음 접속한 경우 안내를 세션당 한 번 자동 표시하고, 사용자가 닫은 뒤에는 반복해서 방해하지 않는다.
5. 일반 Chrome과 Safari의 기존 설치 동작은 그대로 유지하며 서비스 워커 캐시 버전을 갱신한다.

### 성공 기준

- Android 카카오톡 환경에서 `Chrome으로 열기` 버튼과 올바른 Android Intent URL이 제공된다.
- iPhone 카카오톡 환경에서 Safari로 여는 방법과 `링크 복사` 버튼이 제공된다.
- 카카오톡이 아닌 브라우저에서는 기존 설치 팝업 또는 수동 설치 안내가 유지된다.
- 390px 모바일 화면에서 안내창이 잘리지 않고 기존 계산 화면의 한 화면 레이아웃도 유지된다.
- 서비스 워커가 새 캐시 버전을 사용하고 계산·초기화·결과 복사 기능에 회귀가 없다.
